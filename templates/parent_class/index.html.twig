{% extends 'base.html.twig' %}

{% block title %}ParentClass index{% endblock %}

{% block body %}
    <h1>ParentClass index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for parent_class in parent_classes %}
            <tr>
                <td>{{ parent_class.id }}</td>
                <td>{{ parent_class.name }}</td>
                <td>
                    <a href="{{ path('parent_class_show', {'id': parent_class.id}) }}">show</a>
                    <a href="{{ path('parent_class_edit', {'id': parent_class.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('parent_class_new') }}">Create new</a>
{% endblock %}
