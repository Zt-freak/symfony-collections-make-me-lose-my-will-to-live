{% extends 'base.html.twig' %}

{% block title %}ParentClass{% endblock %}

{% block body %}
    <h1>ParentClass</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ parent_class.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ parent_class.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('parent_class_index') }}">back to list</a>

    <a href="{{ path('parent_class_edit', {'id': parent_class.id}) }}">edit</a>

    {{ include('parent_class/_delete_form.html.twig') }}
{% endblock %}
