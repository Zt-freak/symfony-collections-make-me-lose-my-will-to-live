{% extends 'base.html.twig' %}

{% block title %}ChildClass{% endblock %}

{% block body %}
    <h1>ChildClass</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ child_class.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ child_class.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('child_class_index') }}">back to list</a>

    <a href="{{ path('child_class_edit', {'id': child_class.id}) }}">edit</a>

    {{ include('child_class/_delete_form.html.twig') }}
{% endblock %}
