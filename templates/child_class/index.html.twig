{% extends 'base.html.twig' %}

{% block title %}ChildClass index{% endblock %}

{% block body %}
    <h1>ChildClass index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for child_class in child_classes %}
            <tr>
                <td>{{ child_class.id }}</td>
                <td>{{ child_class.name }}</td>
                <td>
                    <a href="{{ path('child_class_show', {'id': child_class.id}) }}">show</a>
                    <a href="{{ path('child_class_edit', {'id': child_class.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('child_class_new') }}">Create new</a>
{% endblock %}
